
/*
This program writes obfuscated C code that, when compiled,
spits out whatever you enter here via stdin:

*/

/*
This C source code spits out the following string using only
calls to the predictable (!) stdlib.h random() function:
"X5O!P%@AP[4\PZX54(P^)7CC)7}$EICAR-STANDARD-ANTIVIRUS-TEST-FILE!$H+H*"
See this GitHub repository for the program that wrote this code:
   https://github.com/rsnbrgr/String-From-Random

*/
#include <stdio.h>
#include <stdlib.h>
int main(void)
    {
    int  bitchar = 0;
    char using   = 0;
    int  new     = 0;

    using=0;
    bitchar=0;
    for (new=0; new<2; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<3; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<6; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<10; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<14; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<15; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<17; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<18; new++) using = (int)random() & 1; bitchar += using;
    printf("%c",bitchar);



    using=0;
    bitchar=0;
    for (new=0; new<19; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<28; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<29; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<30; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<32; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<33; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<34; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<35; new++) using = (int)random() & 1; bitchar += using;
    printf("%c",bitchar);



    using=0;
    bitchar=0;
    for (new=0; new<36; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<37; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<38; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<40; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<42; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<48; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<50; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<51; new++) using = (int)random() & 1; bitchar += using;
    printf("%c",bitchar);



    using=0;
    bitchar=0;
    for (new=0; new<53; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<55; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<57; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<58; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<63; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<64; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<67; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<69; new++) using = (int)random() & 1; bitchar += using;
    printf("%c",bitchar);



    using=0;
    bitchar=0;
    for (new=0; new<74; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<75; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<77; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<79; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<81; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<83; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<84; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<86; new++) using = (int)random() & 1; bitchar += using;
    printf("%c",bitchar);



    using=0;
    bitchar=0;
    for (new=0; new<87; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<88; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<91; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<92; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<93; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<96; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<98; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<99; new++) using = (int)random() & 1; bitchar += using;
    printf("%c",bitchar);



    using=0;
    bitchar=0;
    for (new=0; new<103; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<104; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<105; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<108; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<109; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<110; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<114; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<119; new++) using = (int)random() & 1; bitchar += using;
    printf("%c",bitchar);



    using=0;
    bitchar=0;
    for (new=0; new<123; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<124; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<125; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<127; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<128; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<129; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<130; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<131; new++) using = (int)random() & 1; bitchar += using;
    printf("%c",bitchar);



    using=0;
    bitchar=0;
    for (new=0; new<133; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<136; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<137; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<138; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<140; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<141; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<143; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<145; new++) using = (int)random() & 1; bitchar += using;
    printf("%c",bitchar);



    using=0;
    bitchar=0;
    for (new=0; new<147; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<148; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<150; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<151; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<153; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<156; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<160; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<162; new++) using = (int)random() & 1; bitchar += using;
    printf("%c",bitchar);



    using=0;
    bitchar=0;
    for (new=0; new<164; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<166; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<168; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<169; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<170; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<174; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<176; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<178; new++) using = (int)random() & 1; bitchar += using;
    printf("%c",bitchar);



    using=0;
    bitchar=0;
    for (new=0; new<181; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<184; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<185; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<187; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<188; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<189; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<196; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<200; new++) using = (int)random() & 1; bitchar += using;
    printf("%c",bitchar);



    using=0;
    bitchar=0;
    for (new=0; new<201; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<202; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<205; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<210; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<211; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<212; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<213; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<219; new++) using = (int)random() & 1; bitchar += using;
    printf("%c",bitchar);



    using=0;
    bitchar=0;
    for (new=0; new<220; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<222; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<223; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<224; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<227; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<228; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<229; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<230; new++) using = (int)random() & 1; bitchar += using;
    printf("%c",bitchar);



    using=0;
    bitchar=0;
    for (new=0; new<233; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<235; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<236; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<238; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<240; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<241; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<244; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<245; new++) using = (int)random() & 1; bitchar += using;
    printf("%c",bitchar);



    using=0;
    bitchar=0;
    for (new=0; new<246; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<247; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<250; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<254; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<255; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<258; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<259; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<261; new++) using = (int)random() & 1; bitchar += using;
    printf("%c",bitchar);



    using=0;
    bitchar=0;
    for (new=0; new<262; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<265; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<266; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<268; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<270; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<271; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<272; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<273; new++) using = (int)random() & 1; bitchar += using;
    printf("%c",bitchar);



    using=0;
    bitchar=0;
    for (new=0; new<274; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<276; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<277; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<283; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<284; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<286; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<288; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<290; new++) using = (int)random() & 1; bitchar += using;
    printf("%c",bitchar);



    using=0;
    bitchar=0;
    for (new=0; new<293; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<294; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<296; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<297; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<298; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<299; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<303; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<304; new++) using = (int)random() & 1; bitchar += using;
    printf("%c",bitchar);



    using=0;
    bitchar=0;
    for (new=0; new<305; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<307; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<308; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<310; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<311; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<312; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<313; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<318; new++) using = (int)random() & 1; bitchar += using;
    printf("%c",bitchar);



    using=0;
    bitchar=0;
    for (new=0; new<320; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<321; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<322; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<323; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<324; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<329; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<330; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<333; new++) using = (int)random() & 1; bitchar += using;
    printf("%c",bitchar);



    using=0;
    bitchar=0;
    for (new=0; new<334; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<340; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<344; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<345; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<350; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<351; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<352; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<353; new++) using = (int)random() & 1; bitchar += using;
    printf("%c",bitchar);



    using=0;
    bitchar=0;
    for (new=0; new<354; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<358; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<359; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<366; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<369; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<370; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<373; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<375; new++) using = (int)random() & 1; bitchar += using;
    printf("%c",bitchar);



    using=0;
    bitchar=0;
    for (new=0; new<376; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<378; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<379; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<381; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<383; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<384; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<385; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<390; new++) using = (int)random() & 1; bitchar += using;
    printf("%c",bitchar);



    using=0;
    bitchar=0;
    for (new=0; new<391; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<393; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<394; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<395; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<397; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<399; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<400; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<402; new++) using = (int)random() & 1; bitchar += using;
    printf("%c",bitchar);



    using=0;
    bitchar=0;
    for (new=0; new<403; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<406; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<409; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<411; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<413; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<415; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<416; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<417; new++) using = (int)random() & 1; bitchar += using;
    printf("%c",bitchar);



    using=0;
    bitchar=0;
    for (new=0; new<419; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<423; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<424; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<425; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<426; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<427; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<428; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<430; new++) using = (int)random() & 1; bitchar += using;
    printf("%c",bitchar);



    using=0;
    bitchar=0;
    for (new=0; new<431; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<432; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<441; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<443; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<445; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<446; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<447; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<450; new++) using = (int)random() & 1; bitchar += using;
    printf("%c",bitchar);



    using=0;
    bitchar=0;
    for (new=0; new<452; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<454; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<455; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<456; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<457; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<459; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<461; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<463; new++) using = (int)random() & 1; bitchar += using;
    printf("%c",bitchar);



    using=0;
    bitchar=0;
    for (new=0; new<464; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<468; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<469; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<470; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<473; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<474; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<475; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<476; new++) using = (int)random() & 1; bitchar += using;
    printf("%c",bitchar);



    using=0;
    bitchar=0;
    for (new=0; new<478; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<479; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<482; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<483; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<485; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<489; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<490; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<491; new++) using = (int)random() & 1; bitchar += using;
    printf("%c",bitchar);



    using=0;
    bitchar=0;
    for (new=0; new<493; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<494; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<495; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<496; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<497; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<498; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<499; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<501; new++) using = (int)random() & 1; bitchar += using;
    printf("%c",bitchar);



    using=0;
    bitchar=0;
    for (new=0; new<503; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<504; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<508; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<512; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<514; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<516; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<520; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<521; new++) using = (int)random() & 1; bitchar += using;
    printf("%c",bitchar);



    using=0;
    bitchar=0;
    for (new=0; new<525; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<526; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<528; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<530; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<532; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<534; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<536; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<537; new++) using = (int)random() & 1; bitchar += using;
    printf("%c",bitchar);



    using=0;
    bitchar=0;
    for (new=0; new<539; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<544; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<546; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<549; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<550; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<551; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<554; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<558; new++) using = (int)random() & 1; bitchar += using;
    printf("%c",bitchar);



    using=0;
    bitchar=0;
    for (new=0; new<559; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<562; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<563; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<564; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<566; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<570; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<575; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<576; new++) using = (int)random() & 1; bitchar += using;
    printf("%c",bitchar);



    using=0;
    bitchar=0;
    for (new=0; new<578; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<580; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<582; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<585; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<586; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<588; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<590; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<591; new++) using = (int)random() & 1; bitchar += using;
    printf("%c",bitchar);



    using=0;
    bitchar=0;
    for (new=0; new<592; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<596; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<598; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<600; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<601; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<602; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<604; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<605; new++) using = (int)random() & 1; bitchar += using;
    printf("%c",bitchar);



    using=0;
    bitchar=0;
    for (new=0; new<606; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<607; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<608; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<610; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<612; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<613; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<618; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<620; new++) using = (int)random() & 1; bitchar += using;
    printf("%c",bitchar);



    using=0;
    bitchar=0;
    for (new=0; new<621; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<624; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<625; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<627; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<628; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<629; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<630; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<632; new++) using = (int)random() & 1; bitchar += using;
    printf("%c",bitchar);



    using=0;
    bitchar=0;
    for (new=0; new<633; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<634; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<637; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<638; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<640; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<643; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<644; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<647; new++) using = (int)random() & 1; bitchar += using;
    printf("%c",bitchar);



    using=0;
    bitchar=0;
    for (new=0; new<648; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<649; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<650; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<652; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<655; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<656; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<657; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<658; new++) using = (int)random() & 1; bitchar += using;
    printf("%c",bitchar);



    using=0;
    bitchar=0;
    for (new=0; new<660; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<664; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<665; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<666; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<667; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<668; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<669; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<674; new++) using = (int)random() & 1; bitchar += using;
    printf("%c",bitchar);



    using=0;
    bitchar=0;
    for (new=0; new<675; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<678; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<679; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<682; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<683; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<684; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<687; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<693; new++) using = (int)random() & 1; bitchar += using;
    printf("%c",bitchar);



    using=0;
    bitchar=0;
    for (new=0; new<694; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<695; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<698; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<699; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<700; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<701; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<705; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<706; new++) using = (int)random() & 1; bitchar += using;
    printf("%c",bitchar);



    using=0;
    bitchar=0;
    for (new=0; new<707; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<708; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<714; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<716; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<718; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<719; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<720; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<721; new++) using = (int)random() & 1; bitchar += using;
    printf("%c",bitchar);



    using=0;
    bitchar=0;
    for (new=0; new<722; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<726; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<727; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<728; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<731; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<733; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<734; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<736; new++) using = (int)random() & 1; bitchar += using;
    printf("%c",bitchar);



    using=0;
    bitchar=0;
    for (new=0; new<737; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<738; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<739; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<740; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<744; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<746; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<747; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<750; new++) using = (int)random() & 1; bitchar += using;
    printf("%c",bitchar);



    using=0;
    bitchar=0;
    for (new=0; new<752; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<754; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<758; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<759; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<760; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<762; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<763; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<766; new++) using = (int)random() & 1; bitchar += using;
    printf("%c",bitchar);



    using=0;
    bitchar=0;
    for (new=0; new<768; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<770; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<773; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<779; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<780; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<782; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<785; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<787; new++) using = (int)random() & 1; bitchar += using;
    printf("%c",bitchar);



    using=0;
    bitchar=0;
    for (new=0; new<789; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<790; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<791; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<792; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<794; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<795; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<800; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<801; new++) using = (int)random() & 1; bitchar += using;
    printf("%c",bitchar);



    using=0;
    bitchar=0;
    for (new=0; new<802; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<806; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<807; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<808; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<812; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<813; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<815; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<816; new++) using = (int)random() & 1; bitchar += using;
    printf("%c",bitchar);



    using=0;
    bitchar=0;
    for (new=0; new<819; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<820; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<821; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<822; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<823; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<825; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<827; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<828; new++) using = (int)random() & 1; bitchar += using;
    printf("%c",bitchar);



    using=0;
    bitchar=0;
    for (new=0; new<831; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<832; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<833; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<835; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<836; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<837; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<838; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<840; new++) using = (int)random() & 1; bitchar += using;
    printf("%c",bitchar);



    using=0;
    bitchar=0;
    for (new=0; new<844; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<847; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<848; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<851; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<855; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<856; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<857; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<858; new++) using = (int)random() & 1; bitchar += using;
    printf("%c",bitchar);



    using=0;
    bitchar=0;
    for (new=0; new<859; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<862; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<866; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<868; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<874; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<876; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<880; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<881; new++) using = (int)random() & 1; bitchar += using;
    printf("%c",bitchar);



    using=0;
    bitchar=0;
    for (new=0; new<883; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<886; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<887; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<888; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<892; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<893; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<894; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<897; new++) using = (int)random() & 1; bitchar += using;
    printf("%c",bitchar);



    using=0;
    bitchar=0;
    for (new=0; new<898; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<900; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<902; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<903; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<904; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<907; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<909; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<912; new++) using = (int)random() & 1; bitchar += using;
    printf("%c",bitchar);



    using=0;
    bitchar=0;
    for (new=0; new<913; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<915; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<917; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<918; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<925; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<926; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<927; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<928; new++) using = (int)random() & 1; bitchar += using;
    printf("%c",bitchar);



    using=0;
    bitchar=0;
    for (new=0; new<929; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<931; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<932; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<933; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<934; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<935; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<937; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<938; new++) using = (int)random() & 1; bitchar += using;
    printf("%c",bitchar);



    using=0;
    bitchar=0;
    for (new=0; new<939; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<941; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<943; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<944; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<946; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<947; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<949; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<952; new++) using = (int)random() & 1; bitchar += using;
    printf("%c",bitchar);



    using=0;
    bitchar=0;
    for (new=0; new<953; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<954; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<956; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<958; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<959; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<961; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<963; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<965; new++) using = (int)random() & 1; bitchar += using;
    printf("%c",bitchar);



    using=0;
    bitchar=0;
    for (new=0; new<966; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<967; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<969; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<972; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<973; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<975; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<979; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<981; new++) using = (int)random() & 1; bitchar += using;
    printf("%c",bitchar);



    using=0;
    bitchar=0;
    for (new=0; new<983; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<984; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<985; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<987; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<988; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<991; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<995; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<996; new++) using = (int)random() & 1; bitchar += using;
    printf("%c",bitchar);



    using=0;
    bitchar=0;
    for (new=0; new<998; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<1000; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<1001; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<1002; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<1005; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<1006; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<1008; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<1009; new++) using = (int)random() & 1; bitchar += using;
    printf("%c",bitchar);



    using=0;
    bitchar=0;
    for (new=0; new<1010; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<1013; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<1014; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<1019; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<1022; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<1023; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<1026; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<1027; new++) using = (int)random() & 1; bitchar += using;
    printf("%c",bitchar);



    using=0;
    bitchar=0;
    for (new=0; new<1029; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<1031; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<1036; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<1039; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<1040; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<1044; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<1047; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<1049; new++) using = (int)random() & 1; bitchar += using;
    printf("%c",bitchar);



    using=0;
    bitchar=0;
    for (new=0; new<1050; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<1053; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<1054; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<1058; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<1061; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<1062; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<1063; new++) using = (int)random() & 1; bitchar += using; bitchar <<= 1;
    for (new=0; new<1070; new++) using = (int)random() & 1; bitchar += using;
    printf("%c",bitchar);



    return 0;
    }
